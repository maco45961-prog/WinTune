<Page x:Class="WinNetTools.UI.Views.TracerouteView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BgPrimaryBrush}"
      Foreground="{StaticResource TextPrimaryBrush}">
    <StackPanel Margin="24">
        <TextBlock Text="{StaticResource Traceroute}" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"/>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TargetInput" Text="playvalorant.com" 
                     Background="{StaticResource BgTertiaryBrush}" Foreground="{StaticResource TextPrimaryBrush}"
                     BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="8,6"
                     FontSize="14"/>
            <Button Grid.Column="1" x:Name="RunTraceBtn" Content="{StaticResource RunTraceroute}" Style="{StaticResource PrimaryButton}" Click="RunTrace" Margin="8,0,0,0" IsEnabled="True"/>
            <TextBlock Grid.Column="2" x:Name="TraceLoading" Text="..." Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="8,0,0,0" Visibility="Collapsed"/>
        </Grid>

        <Border Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="0">
            <ListView x:Name="TraceList" Background="Transparent" BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="{StaticResource Hop}" Width="60" DisplayMemberBinding="{Binding Hop}"/>
                        <GridViewColumn Header="{StaticResource Address}" Width="160" DisplayMemberBinding="{Binding Address}"/>
                        <GridViewColumn Header="Hostname" Width="200" DisplayMemberBinding="{Binding Hostname}"/>
                        <GridViewColumn Header="{StaticResource Latency}" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Border CornerRadius="3" Padding="6,2" Margin="2" Background="{StaticResource BgTertiaryBrush}">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="{StaticResource BgTertiaryBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSlowest}" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Foreground="White" FontWeight="Bold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="{Binding LatencyMs}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding TimedOut}" Value="True">
                                                                <Setter Property="Text" Value="*"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                        <TextBlock Text=" ms" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" Margin="2,0,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Foreground="{StaticResource DangerBrush}" FontWeight="Bold" Visibility="{Binding IsSlowest, Converter={x:Null}}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSlowest}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Setter Property="Text" Value="&#x25B2; SLOWEST"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </StackPanel>
</Page>
