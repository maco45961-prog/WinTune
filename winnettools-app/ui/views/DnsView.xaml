<Page x:Class="WinNetTools.UI.Views.DnsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BgPrimaryBrush}"
      Foreground="{StaticResource TextPrimaryBrush}">
    <StackPanel Margin="24">
        <TextBlock Text="{StaticResource CompareDNS}" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"/>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="DnsTargetInput" Text="playvalorant.com"
                     Background="{StaticResource BgTertiaryBrush}" Foreground="{StaticResource TextPrimaryBrush}"
                     BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="8,6"
                     FontSize="14"/>
            <Button Grid.Column="1" x:Name="RunDnsBtn" Content="{StaticResource CompareDNS}" Style="{StaticResource PrimaryButton}" Click="RunDnsTest" Margin="8,0,0,0" IsEnabled="True"/>
            <TextBlock Grid.Column="2" x:Name="DnsLoading" Text="..." Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="8,0,0,0" Visibility="Collapsed"/>
        </Grid>

        <!-- DNS suggestion -->
        <Border x:Name="DnsSuggestionBox" Background="#1A0D98E8" CornerRadius="8" Padding="12" Margin="0,0,0,16" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="DnsSuggestionText" Text="{StaticResource DnsSuggestion}" Foreground="{StaticResource AccentBrush}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="{StaticResource OpenOptimizer}" Style="{StaticResource PrimaryButton}" Click="OpenOptimizerDns" Margin="12,0,0,0"/>
            </Grid>
        </Border>

        <Border Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="0">
            <ListView x:Name="DnsResultsList" Background="Transparent" BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Servidor DNS" Width="140" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Server IP" Width="120" DisplayMemberBinding="{Binding Server}"/>
                        <GridViewColumn Header="Tiempo (ms)" Width="100" DisplayMemberBinding="{Binding ResolveTimeMs}"/>
                        <GridViewColumn Header="IP Resuelta" Width="160" DisplayMemberBinding="{Binding ResolvedIp}"/>
                        <GridViewColumn Header="Estado" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="4,2">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Success}" Value="False">
                                                        <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Foreground="White" FontWeight="Bold" HorizontalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="OK"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Success}" Value="False">
                                                            <Setter Property="Text" Value="FALLO"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </StackPanel>
</Page>
