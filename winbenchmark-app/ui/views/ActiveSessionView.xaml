<Page x:Class="WinBenchmark.UI.Views.ActiveSessionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BgPrimaryBrush}"
      Foreground="{StaticResource TextPrimaryBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <TextBlock Text="{StaticResource ActiveSession}" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"/>

            <!-- Controls -->
            <Border Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Text="{StaticResource DurationSeconds}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="DurationInput" Text="30" Width="80" 
                             Background="{StaticResource BgTertiaryBrush}" Foreground="{StaticResource TextPrimaryBrush}"
                             BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="4" Margin="0,0,8,0" HorizontalAlignment="Left"/>

                    <TextBlock Grid.Row="1" Text="{StaticResource TargetProcess}" VerticalAlignment="Center" Margin="0,8,8,0"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="ProcessInput" Text="VALORANT" 
                             Background="{StaticResource BgTertiaryBrush}" Foreground="{StaticResource TextPrimaryBrush}"
                             BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="4" Margin="0,8,8,0"/>

                    <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,0">
                        <Button x:Name="StartBtn" Content="{StaticResource StartBenchmark}" Style="{StaticResource PrimaryButton}" Click="StartBenchmark" Margin="0,0,8,0" IsEnabled="True"/>
                        <Button x:Name="StopBtn" Content="{StaticResource StopBenchmark}" Style="{StaticResource DangerButton}" Click="StopBenchmark" IsEnabled="False"/>
                    </StackPanel>

                    <TextBlock Grid.Row="2" Grid.ColumnSpan="3" x:Name="StatusText" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,0"/>
                </Grid>
            </Border>

            <!-- Live metrics -->
            <Border Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="CPU" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                        <TextBlock x:Name="LiveCpu" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentBrush}">0%</TextBlock>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="RAM" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                        <TextBlock x:Name="LiveRam" FontSize="24" FontWeight="Bold">0 GB</TextBlock>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Ping" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                        <TextBlock x:Name="LivePing" FontSize="24" FontWeight="Bold">0 ms</TextBlock>
                    </StackPanel>
                    <StackPanel Grid.Column="3">
                        <TextBlock Text="Jitter" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                        <TextBlock x:Name="LiveJitter" FontSize="24" FontWeight="Bold">0 ms</TextBlock>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Session results -->
            <Border x:Name="ResultsBox" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="16" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="Bold" Margin="0,0,0,12">Resultados</TextBlock>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel>
                            <TextBlock Text="{StaticResource AvgFps}" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                            <TextBlock x:Name="ResultFps" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{StaticResource Low1Percent}" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                            <TextBlock x:Name="ResultLow1" FontSize="28" FontWeight="Bold"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="{StaticResource Low01Percent}" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                            <TextBlock x:Name="ResultLow01" FontSize="28" FontWeight="Bold"/>
                        </StackPanel>
                        <StackPanel Grid.Column="3">
                            <TextBlock Text="{StaticResource FrameTimeMs}" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                            <TextBlock x:Name="ResultFrameTime" FontSize="28" FontWeight="Bold"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Margin="0,16,0,0">
                            <TextBlock Text="{StaticResource CpuUsage}" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                            <TextBlock x:Name="ResultCpu" FontSize="18" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,16,0,0">
                            <TextBlock Text="{StaticResource RamUsage}" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                            <TextBlock x:Name="ResultRam" FontSize="18" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,16,0,0">
                            <TextBlock Text="{StaticResource PingMs}" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                            <TextBlock x:Name="ResultPing" FontSize="18" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="3" Margin="0,16,0,0">
                            <TextBlock Text="{StaticResource JitterMs}" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                            <TextBlock x:Name="ResultJitter" FontSize="18" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock x:Name="ResultSource" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,12,0,0"/>
                    <Button x:Name="ExportBtn" Content="{StaticResource GenerateReport}" Style="{StaticResource SecondaryButton}" Click="ExportSession" Margin="0,12,0,0" HorizontalAlignment="Left"/>
                    <TextBlock x:Name="ExportStatus" Foreground="{StaticResource SuccessBrush}" Margin="0,8,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
