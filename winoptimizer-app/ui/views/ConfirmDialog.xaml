<Window x:Class="WinOptimizer.UI.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{StaticResource ConfirmTitle}"
        Width="520" Height="500"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgSecondaryBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="{StaticResource ConfirmTitle}" FontSize="22" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>

        <!-- Message -->
        <TextBlock Grid.Row="1" x:Name="ConfirmMessageText" FontSize="14"
                   Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16"/>

        <!-- Restore point notice -->
        <Border Grid.Row="2" Background="#1a2a3a" CornerRadius="8" Padding="12" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="💾" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="{StaticResource ConfirmRestorePoint}" FontSize="12"
                           Foreground="{StaticResource AccentGreenBrush}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Tweak List -->
        <Border Grid.Row="3" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="TweakListPanel"/>
            </ScrollViewer>
        </Border>

        <!-- Restart notice -->
        <TextBlock Grid.Row="4" x:Name="RestartNotice" Text="{StaticResource ConfirmRestart}"
                   FontSize="12" Foreground="{StaticResource AccentYellowBrush}" Margin="0,0,0,16" Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{StaticResource ConfirmCancel}" Style="{StaticResource CancelButtonStyle}"
                    Margin="0,0,12,0" Click="Cancel_Click"/>
            <Button Content="{StaticResource ConfirmApply}" Style="{StaticResource ConfirmButtonStyle}"
                    Click="Apply_Click"/>
        </StackPanel>
    </Grid>

    <Window.Resources>
        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ConfirmButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentPrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="24,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
</Window>
